import React from 'react'

function AvailableStockPageTable({
    query,
    currentPage,
    selection,
    sort,
}: {
    query: string;
    currentPage: number;
    selection: string;
    sort: string;

}) {
    return (
        <h1>Hii</h1>
    )
}

export default AvailableStockPageTable